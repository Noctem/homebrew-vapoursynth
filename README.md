# homebrew-vapoursynth

Formulae for VapourSynth plugins.

## Installation

To use these, tap this repo: `brew tap noctem/vapoursynth`

Then install your desired plugin(s): `brew install --HEAD vs-[plugin]`
